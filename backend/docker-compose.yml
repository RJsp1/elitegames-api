services:
  api:
    build: .
    ports:
      - "3001:3001"
    env_file:
      - .env
    volumes:
      # Certificados montados em runtime — nunca no image build
      - ./certificates:/app/certificates:ro
    restart: unless-stopped
